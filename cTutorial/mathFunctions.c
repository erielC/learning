#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9);
    double B = pow(2, 4); //2 to the power of 4
    int C = round(3.14);
    int D = ceil(3.14); //round up a number
    int E = floor(3.99); // round down a number
    double F = fabs(-100); //abosolute value
    double G= log(3);
    double H = sin(45);


    return 0;
}